apiVersion: batch/v1
kind: Job
metadata:
  generateName: jobi
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  template:
    spec:
      containers:
        - image: ubuntu
          name: test
          command:
            - "sleep"
            - "60"
      restartPolicy: Never